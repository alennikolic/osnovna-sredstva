version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: os_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: osnovna_sredstva
      MYSQL_USER: os_user
      MYSQL_PASSWORD: os_password
    ports:
      - "3306:3306"
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - db_data:/var/lib/mysql

  web:
    build: .
    container_name: os_web
    restart: always
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
    depends_on:
      - db

volumes:
  db_data:
